<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VILLA - Cloud Logs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { 
            background-color: #050505; 
            color: #e0e0e0; 
            font-family: 'Inter', system-ui, sans-serif;
            padding-top: 50px;
        }

        /* DISEÑO DE TABLA TOTALMENTE DIFERENTE */
        .log-container {
            background: #111;
            border: 1px solid #222;
            border-radius: 20px;
            overflow: hidden;
        }

        .table { margin-bottom: 0; color: #ccc; }
        .table thead { background: #1a1a1a; }
        .table th { border: none; padding: 20px; color: #888; font-size: 0.8rem; text-transform: uppercase; }
        .table td { border-top: 1px solid #222; padding: 20px; }

        /* Seguridad Visual: El texto nunca romperá la tabla */
        .dato-column { 
            word-break: break-all;
            max-width: 400px;
            color: #fff;
            font-weight: 500;
        }

        .input-cyber {
            background: #111;
            border: 1px solid #333;
            color: white;
            border-radius: 12px;
            padding: 12px 20px;
        }
        .input-cyber:focus { background: #151515; border-color: #6200ee; color: white; box-shadow: none; }

        .btn-neon {
            background: #6200ee;
            color: white;
            border: none;
            border-radius: 12px;
            padding: 12px 30px;
            font-weight: bold;
            transition: 0.3s;
        }
        .btn-neon:hover { background: #7722ff; box-shadow: 0 0 20px rgba(98,0,238,0.4); }
    </style>
</head>
<body>

<div class="container" style="max-width: 800px;">
    <div class="mb-5 text-center">
        <h2 class="fw-bold">REPORTE <span style="color: #6200ee;">VILLA</span></h2>
        <p class="text-muted">Nube Railway • Últimos 10 registros</p>
    </div>

    <div class="mb-5">
        <form action="/add" method="POST" class="d-flex gap-3">
            <input type="text" name="dato" class="form-control input-cyber" placeholder="Nueva entrada de datos..." required maxlength="100">
            <button class="btn btn-neon" type="submit">Añadir</button>
        </form>
    </div>

    <div class="log-container">
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th style="width: 20%">ID</th>
                        <th style="width: 60%">CONTENIDO</th>
                        <th style="width: 20%" class="text-end">OPCIONES</th>
                    </tr>
                </thead>
                <tbody>
                    <% registros.forEach(reg => { %>
                        <tr>
                            <td class="text-muted small">#<%= reg.id %></td>
                            <td class="dato-column"><%= reg.dato %></td>
                            <td class="text-end">
                                <a href="/delete/<%= reg.id %>" class="btn btn-sm text-danger opacity-50 text-decoration-none">Eliminar</a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</div>

</body>
</html>
